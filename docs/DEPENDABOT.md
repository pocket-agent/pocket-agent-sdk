# Dependabot (`.github/dependabot.yml`)

Automates dependency updates for npm packages and GitHub Actions.

Edit `.github/dependabot.yml` directly. **CODEOWNERS** requests reviews on workflow changes.

See [WORKFLOWS.md](WORKFLOWS.md) for CI overview.
